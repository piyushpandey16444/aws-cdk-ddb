import * as cdk from '@aws-cdk/core';

import {} from './aws-lambda-stack';

export class AwsCdkApiStack extends cdk.Stack {
  constructor(scope: cdk.Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    
  }
}
